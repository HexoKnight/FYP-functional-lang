(\
  (
    uncurry,
    K,
  ): (
    (bool -> () -> bool) -> ((bool, ()) -> bool),
    bool -> () -> bool,
  )

  (uncurry K) (K true (), ())
)

(
  // uncurry
  (\f: bool -> () -> bool
    \(a, b): (bool, ())
      f a b
  ),
  // K
  (\x:bool \():() x),
)
